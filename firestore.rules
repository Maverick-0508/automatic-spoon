rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to authenticated users and supervisor roles
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
